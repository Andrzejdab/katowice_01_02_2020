def status_counter(statuses,status):

    for value in status:
        count= 0

        ststuses = statuses.values()
        return list(statues
status = {
    "Allon": "online",
    "Bob":  "offline",
     "Eva": "online"
}

a = status_counter(status)